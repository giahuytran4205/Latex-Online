name: Premium Pull-Based Deployment

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Tailscale Connection
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: remote SSH deployment
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TERMUX_HOST }}
          port: ${{ secrets.TERMUX_PORT }}
          username: ${{ secrets.TERMUX_USER }}
          key: ${{ secrets.TERMUX_SSH_KEY }}
          script: |
            PROJECT_DIR="/data/data/com.termux/files/home/latex-online"
            REPO_URL="https://github.com/giahuytran4205/Latex-Online.git"
            
            # Sync code
            if [ -d "$PROJECT_DIR" ]; then
                cd "$PROJECT_DIR"
                git reset --hard
                git pull
            else
                git clone "$REPO_URL" "$PROJECT_DIR"
                cd "$PROJECT_DIR"
            fi
            
            # Execute the smart deployment runner
            chmod +x scripts/deploy.sh
            ./scripts/deploy.sh
